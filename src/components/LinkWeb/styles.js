import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    padding: 10,
    backgroundColor: "#232630",
    marginTop: 60,
    flexDirection: "row",
    alignItems: "center",
  },
  name: {
    color: "#FFF",
    marginLeft: 8,
    fontSize: 18,
    fontWeight: "bold",
  },
});
